数据结构学习--栈

栈是一种简单的数据结构，和队列一样是一种动态集合，且在其上进行delete操作所移除的元素是预先设定的，即所有的操作都是在栈的最后位置（栈顶）进行的。栈的实现是一种后进先出（LIFO）策略，栈上的insert操作称为压入（push），delete操作称为弹出（pop），就好像翻书一样，最先翻过去的书页要想再看一遍就得把其后边的每一页翻回去，才能看到最先翻过去的第一页，而最后一页则直接翻过来就可以了。

栈中不包含任何元素，即栈是空的；如果试图对一个空栈执行弹出操作，则称为栈下溢。栈中的第一个元素叫作栈底元素，最后一个元素叫作栈顶元素，栈是由高地址向低地址分配的。

栈的存储结构有两种：1.顺序存储结构，线性表即数组 2.链式存储结构，链表


数据结构学习--队列

队列和栈一样也是一种简单的数据结构。在其上进行的insert操作被称为是入队，delete操作被称为是出队。队列的实现是一种先进先出（FIFO）策略，就像排队打饭一样，最先排队的人能最先打到饭，而越排到后边的人越晚打到饭。排在第一位的元素叫作队头元素，排在最后一位的元素叫作队尾元素。
队列也有两种存储结构：1.顺序存储结构，线性表即数组 2.链式存储结构，链表

循环队列：队头和队尾进行相连的顺序结构。

栈和队列相关的点：
0.栈和队列的oc代码实现
1.栈和队列互相转换，使用两个栈实现队列和使用两个队列实现栈
2.给出一个文件的绝对路径，需求将其简化。举一个例子，路径是/home/，简化后是/home；路径是/a/./b/../../c/，简化后是/c。
